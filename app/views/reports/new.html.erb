<h1>New report</h1>
<div class="row">
  <div class="col-sm-4">
    <%= simple_form_for @report, html: { class: 'form-horizontal' }, wrapper: :horizontal_form do |f| %>
      <%= f.input :input, as: :text, hint: 'Each username on new line' %>
      <%#= f.input :format, as: :radio_buttons, collection: [['Followers', 'followers']] %>
      <%= f.button :submit %>
    <% end %>
  </div>
</div>